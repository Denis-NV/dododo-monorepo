{
  "name": "@dododo/functions",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "generate": "pnpm sst shell -- drizzle-kit generate --config=drizzle.config.ts",
    "migrate": "pnpm sst shell -- drizzle-kit migrate --config=drizzle.config.ts",
    "migrate:staging": "pnpm sst shell --stage staging -- drizzle-kit migrate --config=drizzle.config.ts"
  },
  "dependencies": {
    "@dododo/core": "workspace:*",
    "@openauthjs/openauth": "^0.4.3",
    "drizzle-orm": "^0.40.0",
    "drizzle-zod": "^0.7.0",
    "express": "^4.21.2",
    "hono": "^4.7.4",
    "postgres": "^3.4.5",
    "serverless-http": "^3.2.0",
    "sst": "*",
    "valibot": "^1.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8",
    "@types/express": "^5.0.0",
    "drizzle-kit": "^0.30.5"
  }
}
